rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS TEMPORALES DE PRUEBA - PERMITEN TODO
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
